language: bash

sudo: false

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

script:
 - bash -c 'shopt -s globstar; shellcheck install.sh'
 - bash -c 'shopt -s globstar; shellcheck SOURCES/logcat'

matrix:
  fast_finish: true
